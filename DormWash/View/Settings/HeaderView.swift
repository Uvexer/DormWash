import SwiftUI

struct HeaderView: View {
    var body: some View {
        Text("Настройки")
            .font(.largeTitle)
            .fontWeight(.bold)
            .foregroundColor(.white)
            .padding(.top, 50)
    }
}
